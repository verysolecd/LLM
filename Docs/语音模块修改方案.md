分工
whisper-command.exe (8084)
✅ 只做一件事：后台轻量监听语音关键词（几乎不占 CPU / 内存，常驻后台）
❌ 不做识别、不转写、不输出文字
whisper-server.exe (8081)
✅ 负责所有核心功能：实时录音识别、文件转写、输入输出、Web 界面
✅ 接收 command 发来的触发指令


请根据以上，设计whisper-server.exe 启动后的右侧管理页面，要求美观，并在右侧加一个whisper-command.exe 的启动开关，下方要有用户自定义命令名字，对应启动whisper-serverr的各个服务